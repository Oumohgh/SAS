#include <stdio.h>
int Maximum(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}
/*
int main() {
    
    int x = 12, y = 8;
    int max = Maximum(x, y);
    printf("Le maximum entre %d et %d est %d\n", x, y, max);
    return 0;
}
*/