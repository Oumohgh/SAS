#include <stdio.h>
int Minimum(int a, int b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}
/*
int main() {
    
    int x = 12, y = 8;
    int max = Minimum(x, y);
    printf("Le minimum entre %d et %d est %d\n", x, y, max);
    return 0;
}
*/
